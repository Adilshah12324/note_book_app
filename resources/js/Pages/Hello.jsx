import React from 'react';

export default function Hello({ message }) {
    return (
        <div>
            <h1>Inertia + React is working!</h1>
            <p>{message}</p>
        </div>
    );
}
